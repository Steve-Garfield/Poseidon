<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poseidon">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".ActWaterOut2"></activity>
        <activity android:name=".ActChangeAll" />
        <activity android:name=".ActBluetooth" />
        <activity android:name=".BluetoothControlling" />
        <activity android:name=".BluetoothPreferencesActivity" />
        <activity android:name=".ActWaterOut" />
        <activity android:name=".ActProductFavorite" />
        <activity android:name=".ActChangeFav" />
        <activity android:name=".ActRegister" />
        <activity android:name=".ActLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize|adjustPan"></activity>
    </application>

</manifest>